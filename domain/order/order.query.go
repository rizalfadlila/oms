package order

const (
	storeOrder       = `insert into orders () values ()`
	storeOrderDetail = `insert into order_details () values ()`
)

const (
	getReportOrder       = `select * from orders`
	getReportOrderDetail = `select * from order_details `
)
